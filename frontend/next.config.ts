import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // Allow calling local backend from dev server if needed via rewrites (optional)
  // We keep minimal config; frontend calls backend via env URL
};

export default nextConfig;
